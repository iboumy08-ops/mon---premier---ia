print("Je m'appelle Ibrahima Sory Camara")

print(2 + 2)

nom = "Ibrahima Sory Camara"
age = 20
print("Bonjour", nom, "! Tu as", age, "ans.")

# === TON PREMIER MODÈLE IA : PRÉDICTION DE NOTE ===
heures = float(input("Combien d'heures tu révises par semaine ? "))
note = 5 + 1.5 * heures
note = min(note, 20)
print(f"Si tu révises {heures}h, tu auras environ {note:.1f}/20 !")

# === IA SANS INTERNET – DONNÉES DANS LE CODE ===
heures = [7, 4, 10, 2]      # ← tes données
notes  = [15.5, 11.0, 18.0, 8.0]

# Calcul simple : moyenne des gains
gain_moyen = 0
for i in range(len(heures)):
    gain_moyen += (notes[i] - 5) / heures[i]
gain_moyen /= len(heures)

# Prédiction
h = float(input("Combien d'heures tu révises ? "))
note_predite = 5 + gain_moyen * h
note_predite = min(note_predite, 20)

print(f"IA locale prédit : {note_predite:.1f}/20")

# === IA EXAM COACH – NIVEAU 2 ===
print("\n" + "="*40)
print("IA QUI TE DIT SI TU VAS RÉUSSIR")
print("="*40)

revision = float(input("Heures révisées cette semaine : "))
sommeil = float(input("Heures de sommeil par nuit : "))
stress = input("Tu stresses ? (oui/non/un peu) : ").lower()

# Calcul du score (sur 100)
score = 0
score += min(revision * 3, 50)      # max 50 pts
score += min(sommeil * 5, 35)       # max 35 pts
if stress == "non":
    score += 15
elif stress == "un peu":
    score += 8
# sinon 0

print(f"\nScore : {score}/100")
if score >= 70:
    print("Tu vas RÉUSSIR ! Continue !")
elif score >= 50:
    print("C’est bien, mais révise + ou dors mieux.")
else:
    print("Attention ! Il faut plus de révision et de sommeil.")

# === GRAPHIQUE DE TON SCORE ===
import matplotlib.pyplot as plt

# Ton score
score = 59  # ← change avec ton vrai score

# Graphique
plt.figure(figsize=(6, 4))
plt.bar(["Ton score"], [score], color='#00C853' if score >= 70 else '#FFC107' if score >= 50 else '#F44336')
plt.ylim(0, 100)
plt.title("IA Exam Coach – Ton score de réussite")
plt.ylabel("Points / 100")
plt.text(0, score + 5, f"{score}/100", ha='center', fontweight='bold')
plt.show()

# === 5 EXERCICES RÉVISION – 15 MIN ===
print("=== EXERCICE 1 : Salut + nom ===")
nom = input("Ton nom ? ")
print("Salut", nom, "!")

print("\n=== EXERCICE 2 : Calcul simple ===")
a = float(input("Nombre 1 ? "))
b = float(input("Nombre 2 ? "))
print("Somme =", a + b)

print("\n=== EXERCICE 3 : Note sur 20 ===")
note = float(input("Ta note ? "))
if note >= 12:
    print("Tu as réussi !")
else:
    print("Il faut réviser plus.")

print("\n=== EXERCICE 4 : Âge en 2030 ===")
age = int(input("Ton âge ? "))
print("En 2030, tu auras", age + 5, "ans")

print("\n=== EXERCICE 5 : Mot de passe ===")
mdp = input("Mot de passe ? ")
if mdp == "guinee2025":
    print("Accès autorisé !")
else:
    print("Accès refusé.")

# === SEMAINE 1 : BASES PYTHON ===
print("SEMAINE 1 – EXERCICES")

# 1. Variables + print
nom = "Ibrahima"
age = 18
print("Je m'appelle", nom, "et j'ai", age, "ans")

# 2. Input + calcul
heures = float(input("Heures révisées ? "))
note = 5 + 1.5 * heures
print("Note prédite :", note)

# 3. if/else
if note >= 12:
    print("Réussi !")
else:
    print("À revoir")

# 4. Plusieurs conditions
stress = input("Stress ? (oui/non) ")
if stress == "non":
    print("Bonus +2 !")
    note += 2
print("Note finale :", note)

# 5. Mini-coach
print("Conseil :", end=" ")
if note >= 15:
    print("Continue comme ça !")
elif note >= 10:
    print("Bien, mais dors plus.")
else:
    print("Réviser + dormir !")

# === SEMAINE 1 : EXERCICES D'APPLICATION ===
print("EXERCICES RÉELS – COMME UN DEV IA")

# ————————————————————————
# EXERCICE 1 : Calculateur de bourse d’études
# ————————————————————————
print("\n1. BOURSE D'ÉTUDES")
note_math = float(input("Note en maths ? "))
note_phys = float(input("Note en physique ? "))
moyenne = (note_math + note_phys) / 2

if moyenne >= 16:
    bourse = 500000
elif moyenne >= 14:
    bourse = 300000
elif moyenne >= 12:
    bourse = 150000
else:
    bourse = 0

print(f"→ Moyenne : {moyenne:.1f} → Bourse : {bourse} GNF")

# ————————————————————————
# EXERCICE 2 : Gestionnaire de mots de passe
# ————————————————————————
print("\n2. MOT DE PASSE SÉCURISÉ")
mdp = input("Crée un mot de passe : ")
longueur = len(mdp)
force = 0

if longueur >= 8:
    force += 1
if any(c.isupper() for c in mdp):
    force += 1
if any(c.islower() for c in mdp):
    force += 1
if any(c.isdigit() for c in mdp):
    force += 1

print(f"→ Force : {force}/4 → ", end="")
if force == 4:
    print("SÉCURISÉ")
elif force >= 2:
    print("MOYEN")
else:
    print("FAIBLE")

# ————————————————————————
# EXERCICE 3 : Convertisseur de température
# ————————————————————————
print("\n3. CONVERSION °C ↔ °F")
temp = float(input("Température ? "))
unite = input("Unité (C ou F) ? ").upper()

if unite == "C":
    f = temp * 9/5 + 32
    print(f"{temp}°C = {f:.1f}°F")
elif unite == "F":
    c = (temp - 32) * 5/9
    print(f"{temp}°F = {c:.1f}°C")
else:
    print("Unité inconnue")

# ————————————————————————
# EXERCICE 4 : Simulateur de prêt étudiant
# ————————————————————————
print("\n4. PRÊT ÉTUDIANT")
montant = float(input("Montant demandé (GNF) ? "))
revenu = float(input("Revenu familial mensuel ? "))

if revenu >= 2000000:
    taux = 5
elif revenu >= 1000000:
    taux = 8
else:
    taux = 12

interet = montant * taux / 100
print(f"→ Taux : {taux}% → Intérêt : {interet:,.0f} GNF")

# ————————————————————————
# EXERCICE 5 : IA Coach Révision (version pro)
# ————————————————————————
print("\n5. IA COACH RÉVISION")
heures = float(input("Heures révisées aujourd'hui ? "))
objectif = 6

if heures >= objectif:
    print("OBJECTIF ATTEINT !")
    bonus = heures - objectif
    print(f"Bonus : +{bonus}h → Tu gagnes en confiance !")
else:
    manque = objectif - heures
    print(f"IL MANQUE {manque}h")
    if manque <= 1:
        print("Tu peux rattraper ce soir !")
    else:
        print("Planifie mieux demain.")

# === SEMAINE 2 : BOUCLES + LISTES – APPLICATIONS RÉELLES ===
print("SEMAINE 2 – DÉVELOPPEUR JUNIOR")

# ————————————————————————
# EXERCICE 1 : Gestion de classe (10 étudiants)
# ————————————————————————
print("\n1. GESTION DE CLASSE")
notes = [12, 18, 15, 9, 19, 14, 17, 11, 20, 13]

print("Toutes les notes :", notes)
print("Nombre d'étudiants :", len(notes))

moyenne_classe = sum(notes) / len(notes)
print(f"Moyenne classe : {moyenne_classe:.2f}/20")

# ————————————————————————
# EXERCICE 2 : Top 3 + Redoublants
# ————————————————————————
print("\n2. CLASSEMENT")
notes_triees = sorted(notes, reverse=True)
print("Top 3 :", notes_triees[:3])

print("Redoublants (<10) :", end=" ")
for note in notes:
    if note < 10:
        print(note, end=" ")
print()  # ligne vide

# ————————————————————————
# EXERCICE 3 : Suivi quotidien de révision
# ————————————————————————
print("\n3. SUIVI 7 JOURS")
revisions = []

for jour in range(1, 8):
    h = float(input(f"Jour {jour} – heures révisées ? "))
    revisions.append(h)

total = sum(revisions)
print(f"Total semaine : {total}h → {total/7:.1f}h/jour en moyenne")

# ————————————————————————
# EXERCICE 4 : Recherche du meilleur jour
# ————————————————————————
print("\n4. MEILLEUR JOUR")
meilleur = max(revisions)
jour_meilleur = revisions.index(meilleur) + 1
print(f"Meilleur jour : Jour {jour_meilleur} → {meilleur}h")

# ————————————————————————
# EXERCICE 5 : IA Motivation (pro)
# ————————————————————————
print("\n5. IA MOTIVATION PRO")
if total >= 40:
    print("CHAMPION ! Tu es dans le top 1% des étudiants guinéens")
elif total >= 30:
    print("EXCELLENT ! Continue, tu vas tout déchirer")
elif total >= 20:
    print("C’EST BIEN ! Ajoute 1h/jour et tu passes pro")
else:
    print("ALERTE ! Plan d’urgence : 6h/jour minimum cette semaine")